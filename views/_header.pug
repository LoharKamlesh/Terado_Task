header.header 
    nav.nav.nav--tours
        a.nav__el(href='/overview')
            img#terado__img(src='/img/terado.png')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log out
        else
            a.nav__el(href='/login') Log in 
            a.nav__el.nav__el--cta(href='/signup') Sign up